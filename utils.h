#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

extern int countLines(char* filename);
char *ltrim(char *s);

char *rtrim(char *s);

char *trim(char *s);
void removeSpaces(char *str) ;